# Codex Installation for Notify Email

To use this skill with Codex:

1.  Add the path to this repository in your Codex `AGENTS.md` configuration under the `skills` section.
2.  Enable the notification hook by adding `node scripts/notify.js` as a post-task step.
3.  Configure your credentials in `~/.notify-email/config.json`.
